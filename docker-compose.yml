services:
  mongo:
    image: mongo:7
    container_name: manuscript-writer-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: manuscript_writer

volumes:
  mongo_data:

